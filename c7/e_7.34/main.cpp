#include <bits/stdc++.h>

using namespace std;

int height;

class Screen
      {
      public:
            typedef string::size_type pos;
            void dummy_fcn(pos height)
              {
                auto cursor = width * height;
              }
//            typedef string::size_type pos;
      private:
            int width;

      };

int main ()
try
  {
    return 0;
  }
catch (exception &e)
  {
    cerr << "Error: " << e.what() << endl;
  }
